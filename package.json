{
  "name": "which-key-code",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rm dist/*",
    "dev": "parcel serve src/*.html",
    "bundle": "parcel build src/*.html --public-url ./",
    "build": "run-s clean bundle",
    "deploy": "gh-pages -d dist",
    "test:ci": "start-test dev :1234 test",
    "test": "cypress run"
  },
  "dependencies": {
    "tailwindcss": "^1.5.2"
  },
  "devDependencies": {
    "autoprefixer": "^9.8.5",
    "cypress": "^4.11.0",
    "gh-pages": "^3.1.0",
    "npm-run-all": "^4.1.5",
    "parcel-bundler": "^1.12.4",
    "postcss-modules": "^3.2.0",
    "prettier": "^2.0.5",
    "start-server-and-test": "^1.11.2",
    "typescript": "^3.9.7"
  }
}
